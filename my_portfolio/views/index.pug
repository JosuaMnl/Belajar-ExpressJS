extends layout

block content
  h1.text-center.text-danger= title
  p.text-center.text-danger Welcome to #{title}
  form(action="") 
    div.mb-3
      label.form-label Nama
      input.form-control  
    div.mb-3
      label.form-label Password
      input(type="password").form-control
    button.btn.btn-primary Submit